# Local Runtime Entry Points

This folder is the local consumer layer for this workspace.

Keep only:

- operational scripts that we run from this private repo
- private runtime glue, launch code, and local entrypoints

Do not keep reusable engine implementation here.

The maintained engine lives in `packages/openfeed/src/core/`.
