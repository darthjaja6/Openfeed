# Local Web Feed Example

This is the configuration used by the main Quickstart. It reads from TikTok and
pushes cards to OpenFeed's built-in localhost web consumer via
`consumer_type: local_web`.

Follow the full setup in [the main Quickstart](../../README.md#quickstart).
